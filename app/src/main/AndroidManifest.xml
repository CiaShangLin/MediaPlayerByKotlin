<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shang.mediaplayerbykotlin">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MP.MediaPlayerService"
            android:exported="false">

        </service>

        <activity android:name=".PlayMusicActivity">
            
        </activity>
        
        <receiver android:name=".PlayMusicActivity$MyReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.shang.mediaplayerbykotlin.START"></action>
                <action android:name="com.shang.mediaplayerbykotlin.STOP"></action>
                <action android:name="com.shang.mediaplayerbykotlin.RESET"></action>
                <action android:name="com.shang.mediaplayerbykotlin.RESTART"></action>
                <action android:name="com.shang.mediaplayerbykotlin.NEXT"></action>
                <action android:name="com.shang.mediaplayerbykotlin.PREVIOUS"></action>
                <action android:name="com.shang.mediaplayerbykotlin.MODE"></action>
                <action android:name="com.shang.mediaplayerbykotlin.CURRENT_TIME"></action>
            </intent-filter>
        </receiver>
    </application>

</manifest>